[package]
name = "tuiframe-core"
version = "0.1.0"
edition = "2024"
description = "Core library for tuiframe — file-driven TUI component catalog"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"
walkdir = "2"

[dev-dependencies]
tempfile = "3"
